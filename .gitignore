<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title>Tombol Ringan</title>
  <style>
    body {
      margin: 0;
      padding: 40px;
      background: #111;
      font-family: Arial, sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .menu {
      display: flex;
      gap: 16px;
    }

    .menu button {
      padding: 12px 24px;
      background: transparent;
      color: #fff;
      border: 2px solid #00f0ff;
      border-radius: 10px;
      font-weight: bold;
      font-size: 15px;
      cursor: pointer;
      position: relative;
      transition: all 0.3s ease;
    }

    .menu button:hover {
      background: #00f0ff;
      color: #111;
      box-shadow: 0 0 12px #00f0ff;
      transform: translateY(-2px);
    }
  </style>
</head>
<body>

  <div class="menu">
    <button>Dashboard</button>
    <button>About</button>
    <button>FAQ</button>
  </div>

</body>
</html>
